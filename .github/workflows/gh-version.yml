name: Create GitHub Version
on:
  push:
    branches:
      - 'release/**'
    paths-ignore:
      - 'docs/**'
      - '**.md'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          # Needed to get all tags
          fetch-depth: 0
      - name: Tag
        uses: ./.github/actions/gh-version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
